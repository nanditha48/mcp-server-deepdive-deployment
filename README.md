```markdown
## MCP Servers Configuration

```json
{
    "mcpServers": {
        "Demo": {
            "command": "uvx",
            "args": [
                "--from",
                "git+https://github.com/nanditha48/mcp-server-deepdive-deployment.git",
                "mcp-server"
            ]
        }
    }
}
```